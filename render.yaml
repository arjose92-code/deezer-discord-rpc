services:
  - type: web
    name: deezerrp-cloud
    env: python
    region: oregon
    plan: free
    branch: main
    rootDir: cloud
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
